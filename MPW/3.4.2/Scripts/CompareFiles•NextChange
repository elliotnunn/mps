Set Exit 0
Find \•[ ∂t]*Find \:/∞/ "{{TempFolder}}"Compare_File
if {Status}
	Beep
	Confirm "No more changes.  Are you done comparing these files?" && CompareFiles•Quit
else
	Mark -y § Current_Line "{{TempFolder}}"Compare_File
	Execute "{{TempFolder}}Compare_File.§"
end
