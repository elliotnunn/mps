SC Memory.c -o Memory.c.o -w 17 -proto strict -sym Full -D OLDROUTINELOCATIONS=0
Asm DAEntry.a -o DAEntry.a.o 
Link -sym Full -da -rt DRVR=12 ∂
	-m DAEntry -sg Memory				# DAEntry is located in DAEntry.a.o ∂
	DAEntry.a.o							# This must preceed DRVRRuntime.o ∂
	"{Libraries}"DRVRRuntime.o			# This must preceed MacRuntime.o ∂
	Memory.c.o ∂
	"{Libraries}"MacRuntime.o ∂
	"{Libraries}"Interface.o ∂
	-o Memory -c 'movr' -t 'dfil'
