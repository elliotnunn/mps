;
;    File:       IBCarbonRuntime.a
;
;    Contains:   Nib support for Carbon
;
;    Version:    Technology: CarbonLib 1.1/Mac OS X
;                Release:    Universal Interfaces 3.4
;
;    Copyright:  © 2000-2001 by Apple Computer, Inc., all rights reserved.
;
;    Bugs?:      For bug reports, consult the following page on
;                the World Wide Web:
;
;                    http://developer.apple.com/bugreporter/
;
;
	IF &TYPE('__IBCARBONRUNTIME__') = 'UNDEFINED' THEN
__IBCARBONRUNTIME__ SET 1

	IF &TYPE('__CFSTRING__') = 'UNDEFINED' THEN
	include 'CFString.a'
	ENDIF
	IF &TYPE('__CFBUNDLE__') = 'UNDEFINED' THEN
	include 'CFBundle.a'
	ENDIF
	IF &TYPE('__MACWINDOWS__') = 'UNDEFINED' THEN
	include 'MacWindows.a'
	ENDIF
	IF &TYPE('__CONTROLDEFINITIONS__') = 'UNDEFINED' THEN
	include 'ControlDefinitions.a'
	ENDIF

kIBCarbonRuntimeCantFindNibFile	EQU		-10960
kIBCarbonRuntimeObjectNotOfRequestedType EQU -10961
kIBCarbonRuntimeCantFindObject	EQU		-10962
;  ----- typedef ------ 


;  ----- Create & Dispose NIB References ------ 

; *  CreateNibReference()
; *  
; *  Availability:
; *    Non-Carbon CFM:   not available
; *    CarbonLib:        in CarbonLib 1.1 and later
; *    Mac OS X:         in version 10.0 or later
; 

;
; extern OSStatus CreateNibReference(CFStringRef inNibName, IBNibRef *outNibRef)
;
	IF TARGET_OS_MAC ** TARGET_RT_MAC_CFM THEN
		IMPORT_CFM_FUNCTION CreateNibReference
	ENDIF


; *  CreateNibReferenceWithCFBundle()
; *  
; *  Availability:
; *    Non-Carbon CFM:   not available
; *    CarbonLib:        in CarbonLib 1.1 and later
; *    Mac OS X:         in version 10.0 or later
; 

;
; extern OSStatus CreateNibReferenceWithCFBundle(CFBundleRef inBundle, CFStringRef inNibName, IBNibRef *outNibRef)
;
	IF TARGET_OS_MAC ** TARGET_RT_MAC_CFM THEN
		IMPORT_CFM_FUNCTION CreateNibReferenceWithCFBundle
	ENDIF


; *  DisposeNibReference()
; *  
; *  Availability:
; *    Non-Carbon CFM:   not available
; *    CarbonLib:        in CarbonLib 1.1 and later
; *    Mac OS X:         in version 10.0 or later
; 

;
; extern void DisposeNibReference(IBNibRef inNibRef)
;
	IF TARGET_OS_MAC ** TARGET_RT_MAC_CFM THEN
		IMPORT_CFM_FUNCTION DisposeNibReference
	ENDIF

;  ----- Window ------ 

; *  CreateWindowFromNib()
; *  
; *  Availability:
; *    Non-Carbon CFM:   not available
; *    CarbonLib:        in CarbonLib 1.1 and later
; *    Mac OS X:         in version 10.0 or later
; 

;
; extern OSStatus CreateWindowFromNib(IBNibRef inNibRef, CFStringRef inName, WindowRef *outWindow)
;
	IF TARGET_OS_MAC ** TARGET_RT_MAC_CFM THEN
		IMPORT_CFM_FUNCTION CreateWindowFromNib
	ENDIF

;  ----- Menu -----


; *  CreateMenuFromNib()
; *  
; *  Availability:
; *    Non-Carbon CFM:   not available
; *    CarbonLib:        in CarbonLib 1.1 and later
; *    Mac OS X:         in version 10.0 or later
; 

;
; extern OSStatus CreateMenuFromNib(IBNibRef inNibRef, CFStringRef inName, MenuRef *outMenuRef)
;
	IF TARGET_OS_MAC ** TARGET_RT_MAC_CFM THEN
		IMPORT_CFM_FUNCTION CreateMenuFromNib
	ENDIF

;  ----- MenuBar ------


; *  CreateMenuBarFromNib()
; *  
; *  Availability:
; *    Non-Carbon CFM:   not available
; *    CarbonLib:        in CarbonLib 1.1 and later
; *    Mac OS X:         in version 10.0 or later
; 

;
; extern OSStatus CreateMenuBarFromNib(IBNibRef inNibRef, CFStringRef inName, Handle *outMenuBar)
;
	IF TARGET_OS_MAC ** TARGET_RT_MAC_CFM THEN
		IMPORT_CFM_FUNCTION CreateMenuBarFromNib
	ENDIF


; *  SetMenuBarFromNib()
; *  
; *  Availability:
; *    Non-Carbon CFM:   not available
; *    CarbonLib:        in CarbonLib 1.1 and later
; *    Mac OS X:         in version 10.0 or later
; 

;
; extern OSStatus SetMenuBarFromNib(IBNibRef inNibRef, CFStringRef inName)
;
	IF TARGET_OS_MAC ** TARGET_RT_MAC_CFM THEN
		IMPORT_CFM_FUNCTION SetMenuBarFromNib
	ENDIF

	ENDIF ; __IBCARBONRUNTIME__ 

