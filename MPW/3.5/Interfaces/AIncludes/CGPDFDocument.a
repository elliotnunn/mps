;
;    File:       CGPDFDocument.a
;
;    Contains:   xxx put contents here xxx
;
;    Version:    Technology: from CoreGraphics-70.root
;                Release:    Universal Interfaces 3.4
;
;    Copyright:  © 2000-2001 by Apple Computer, Inc., all rights reserved.
;
;    Bugs?:      For bug reports, consult the following page on
;                the World Wide Web:
;
;                    http://developer.apple.com/bugreporter/
;
;

	IF &TYPE('__CGPDFDOCUMENT__') = 'UNDEFINED' THEN
__CGPDFDOCUMENT__ SET 1

	IF &TYPE('__CGBASE__') = 'UNDEFINED' THEN
	include 'CGBase.a'
	ENDIF
	IF &TYPE('__CGDATAPROVIDER__') = 'UNDEFINED' THEN
	include 'CGDataProvider.a'
	ENDIF
	IF &TYPE('__CGGEOMETRY__') = 'UNDEFINED' THEN
	include 'CGGeometry.a'
	ENDIF
	IF &TYPE('__CFURL__') = 'UNDEFINED' THEN
	include 'CFURL.a'
	ENDIF



;  Create a PDF document, using `provider' to obtain the document's
; * data. 

; *  CGPDFDocumentCreateWithProvider()
; *  
; *  Availability:
; *    Non-Carbon CFM:   not available
; *    CarbonLib:        not available
; *    Mac OS X:         in version 10.0 or later
; 

;
; extern CGPDFDocumentRef CGPDFDocumentCreateWithProvider(CGDataProviderRef provider)
;
	IF TARGET_OS_MAC ** TARGET_RT_MAC_CFM THEN
		IMPORT_CFM_FUNCTION CGPDFDocumentCreateWithProvider
	ENDIF

;  Create a PDF document from `url'. 

; *  CGPDFDocumentCreateWithURL()
; *  
; *  Availability:
; *    Non-Carbon CFM:   not available
; *    CarbonLib:        not available
; *    Mac OS X:         in version 10.0 or later
; 

;
; extern CGPDFDocumentRef CGPDFDocumentCreateWithURL(CFURLRef url)
;
	IF TARGET_OS_MAC ** TARGET_RT_MAC_CFM THEN
		IMPORT_CFM_FUNCTION CGPDFDocumentCreateWithURL
	ENDIF

;  Increment the retain count of `document' and return it.  All PDF
; * documents are created with an initial retain count of 1. 

; *  CGPDFDocumentRetain()
; *  
; *  Availability:
; *    Non-Carbon CFM:   not available
; *    CarbonLib:        not available
; *    Mac OS X:         in version 10.0 or later
; 

;
; extern CGPDFDocumentRef CGPDFDocumentRetain(CGPDFDocumentRef document)
;
	IF TARGET_OS_MAC ** TARGET_RT_MAC_CFM THEN
		IMPORT_CFM_FUNCTION CGPDFDocumentRetain
	ENDIF

;  Decrement the retain count of `document'.  If the retain count reaches 0,
; * then free it and any associated resources. 

; *  CGPDFDocumentRelease()
; *  
; *  Availability:
; *    Non-Carbon CFM:   not available
; *    CarbonLib:        not available
; *    Mac OS X:         in version 10.0 or later
; 

;
; extern void CGPDFDocumentRelease(CGPDFDocumentRef document)
;
	IF TARGET_OS_MAC ** TARGET_RT_MAC_CFM THEN
		IMPORT_CFM_FUNCTION CGPDFDocumentRelease
	ENDIF

;  Return the number of pages in `document'. 

; *  CGPDFDocumentGetNumberOfPages()
; *  
; *  Availability:
; *    Non-Carbon CFM:   not available
; *    CarbonLib:        not available
; *    Mac OS X:         in version 10.0 or later
; 

;
; extern int CGPDFDocumentGetNumberOfPages(CGPDFDocumentRef document)
;
	IF TARGET_OS_MAC ** TARGET_RT_MAC_CFM THEN
		IMPORT_CFM_FUNCTION CGPDFDocumentGetNumberOfPages
	ENDIF

;  Return the media box of page number `page' in `document'. 

; *  CGPDFDocumentGetMediaBox()
; *  
; *  Availability:
; *    Non-Carbon CFM:   not available
; *    CarbonLib:        not available
; *    Mac OS X:         in version 10.0 or later
; 

;
; extern CGRect CGPDFDocumentGetMediaBox(CGPDFDocumentRef document, int page)
;
	IF TARGET_OS_MAC ** TARGET_RT_MAC_CFM THEN
		IMPORT_CFM_FUNCTION CGPDFDocumentGetMediaBox
	ENDIF

;  Return the crop box of page number `page' in `document'. 

; *  CGPDFDocumentGetCropBox()
; *  
; *  Availability:
; *    Non-Carbon CFM:   not available
; *    CarbonLib:        not available
; *    Mac OS X:         in version 10.0 or later
; 

;
; extern CGRect CGPDFDocumentGetCropBox(CGPDFDocumentRef document, int page)
;
	IF TARGET_OS_MAC ** TARGET_RT_MAC_CFM THEN
		IMPORT_CFM_FUNCTION CGPDFDocumentGetCropBox
	ENDIF

;  Return the bleed box of page number `page' in `document'. 

; *  CGPDFDocumentGetBleedBox()
; *  
; *  Availability:
; *    Non-Carbon CFM:   not available
; *    CarbonLib:        not available
; *    Mac OS X:         in version 10.0 or later
; 

;
; extern CGRect CGPDFDocumentGetBleedBox(CGPDFDocumentRef document, int page)
;
	IF TARGET_OS_MAC ** TARGET_RT_MAC_CFM THEN
		IMPORT_CFM_FUNCTION CGPDFDocumentGetBleedBox
	ENDIF

;  Return the trim box of page number `page' in `document'. 

; *  CGPDFDocumentGetTrimBox()
; *  
; *  Availability:
; *    Non-Carbon CFM:   not available
; *    CarbonLib:        not available
; *    Mac OS X:         in version 10.0 or later
; 

;
; extern CGRect CGPDFDocumentGetTrimBox(CGPDFDocumentRef document, int page)
;
	IF TARGET_OS_MAC ** TARGET_RT_MAC_CFM THEN
		IMPORT_CFM_FUNCTION CGPDFDocumentGetTrimBox
	ENDIF

;  Return the art box of page number `page' in `document'. 

; *  CGPDFDocumentGetArtBox()
; *  
; *  Availability:
; *    Non-Carbon CFM:   not available
; *    CarbonLib:        not available
; *    Mac OS X:         in version 10.0 or later
; 

;
; extern CGRect CGPDFDocumentGetArtBox(CGPDFDocumentRef document, int page)
;
	IF TARGET_OS_MAC ** TARGET_RT_MAC_CFM THEN
		IMPORT_CFM_FUNCTION CGPDFDocumentGetArtBox
	ENDIF

;  Return the rotation angle (in degrees) of page number `page' in
; * `document'. 

; *  CGPDFDocumentGetRotationAngle()
; *  
; *  Availability:
; *    Non-Carbon CFM:   not available
; *    CarbonLib:        not available
; *    Mac OS X:         in version 10.0 or later
; 

;
; extern int CGPDFDocumentGetRotationAngle(CGPDFDocumentRef document, int page)
;
	IF TARGET_OS_MAC ** TARGET_RT_MAC_CFM THEN
		IMPORT_CFM_FUNCTION CGPDFDocumentGetRotationAngle
	ENDIF

	ENDIF ; __CGPDFDOCUMENT__ 

