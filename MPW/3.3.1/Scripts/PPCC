Set Echo 0
Set Exit 0
("PPCC.driver" {"Parameters"} ) > "{TempFolder}PPCC.tmp"
if ({status})
	Set Exit 1
	Exit 1
end
"{TempFolder}PPCC.tmp"
if ({status})
	Set Exit 1
	Exit 1
end
(Delete -y "{TempFolder}PPCC.tmp") ∑∑ Dev:Null
