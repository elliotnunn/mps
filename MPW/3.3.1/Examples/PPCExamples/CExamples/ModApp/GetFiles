# Utility script to check out all of ModApp's files from its nested projects
# (This is mostly useful to the team which is building the Fast Track CD)
#
# Usage:
#   GetFiles [revision-name]
#

# was a revision specified?
if ("{1}" != "")
	set get-options "{1}"
else
	set get-options "-a"
end

# Set our check-out directories
CheckOutDir -project ModApp∫ :
CheckOutDir -project ModApp∫ModuleSources∫ :ModuleSources:

# Construct our folder structure
CheckOut MakeFolders
MakeFolders

# Get the files
CheckOut -r "{get-options}"