;
;    File:       Carbon.a
;
;    Contains:   Master include for all of Carbon
;
;    Version:    Technology: Mac OS X
;                Release:    Universal Interfaces 3.4
;
;    Copyright:  © 2000-2001 by Apple Computer, Inc., all rights reserved.
;
;    Bugs?:      For bug reports, consult the following page on
;                the World Wide Web:
;
;                    http://developer.apple.com/bugreporter/
;
;
	IF &TYPE('__CARBON__') = 'UNDEFINED' THEN
__CARBON__ SET 1

	IF &TYPE('__CORESERVICES__') = 'UNDEFINED' THEN
	include 'CoreServices.a'
	ENDIF
	IF &TYPE('__APPLICATIONSERVICES__') = 'UNDEFINED' THEN
	include 'ApplicationServices.a'
	ENDIF

	IF &TYPE('__APPLICATIONSERVICES__') = 'UNDEFINED' THEN
	include 'ApplicationServices.a'
	ENDIF
	IF &TYPE('__BALLOONS__') = 'UNDEFINED' THEN
	include 'Balloons.a'
	ENDIF
	IF &TYPE('__EVENTS__') = 'UNDEFINED' THEN
	include 'Events.a'
	ENDIF
	IF &TYPE('__PROCESSES__') = 'UNDEFINED' THEN
	include 'Processes.a'
	ENDIF
	IF &TYPE('__NOTIFICATION__') = 'UNDEFINED' THEN
	include 'Notification.a'
	ENDIF
	IF &TYPE('__DRAG__') = 'UNDEFINED' THEN
	include 'Drag.a'
	ENDIF
	IF &TYPE('__ICONS__') = 'UNDEFINED' THEN
	include 'Icons.a'
	ENDIF
	IF &TYPE('__CONTROLS__') = 'UNDEFINED' THEN
	include 'Controls.a'
	ENDIF
	IF &TYPE('__APPEARANCE__') = 'UNDEFINED' THEN
	include 'Appearance.a'
	ENDIF
	IF &TYPE('__MACWINDOWS__') = 'UNDEFINED' THEN
	include 'MacWindows.a'
	ENDIF
	IF &TYPE('__TEXTEDIT__') = 'UNDEFINED' THEN
	include 'TextEdit.a'
	ENDIF
	IF &TYPE('__MENUS__') = 'UNDEFINED' THEN
	include 'Menus.a'
	ENDIF
	IF &TYPE('__DIALOGS__') = 'UNDEFINED' THEN
	include 'Dialogs.a'
	ENDIF
	IF &TYPE('__LISTS__') = 'UNDEFINED' THEN
	include 'Lists.a'
	ENDIF
	IF &TYPE('__CARBONEVENTS__') = 'UNDEFINED' THEN
	include 'CarbonEvents.a'
	ENDIF
	IF &TYPE('__TEXTSERVICES__') = 'UNDEFINED' THEN
	include 'TextServices.a'
	ENDIF
	IF &TYPE('__SCRAP__') = 'UNDEFINED' THEN
	include 'Scrap.a'
	ENDIF
	IF &TYPE('__MACTEXTEDITOR__') = 'UNDEFINED' THEN
	include 'MacTextEditor.a'
	ENDIF
	IF &TYPE('__MACHELP__') = 'UNDEFINED' THEN
	include 'MacHelp.a'
	ENDIF
	IF &TYPE('__CONTROLDEFINITIONS__') = 'UNDEFINED' THEN
	include 'ControlDefinitions.a'
	ENDIF
	IF &TYPE('__TSMTE__') = 'UNDEFINED' THEN
	include 'TSMTE.a'
	ENDIF
	IF &TYPE('__TRANSLATIONEXTENSIONS__') = 'UNDEFINED' THEN
	include 'TranslationExtensions.a'
	ENDIF
	IF &TYPE('__TRANSLATION__') = 'UNDEFINED' THEN
	include 'Translation.a'
	ENDIF
	IF &TYPE('__AEINTERACTION__') = 'UNDEFINED' THEN
	include 'AEInteraction.a'
	ENDIF
	IF &TYPE('__TYPESELECT__') = 'UNDEFINED' THEN
	include 'TypeSelect.a'
	ENDIF
	IF &TYPE('__INTERNETCONFIG__') = 'UNDEFINED' THEN
	include 'InternetConfig.a'
	ENDIF
	IF &TYPE('__MACAPPLICATION__') = 'UNDEFINED' THEN
	include 'MacApplication.a'
	ENDIF
	IF &TYPE('__KEYBOARDS__') = 'UNDEFINED' THEN
	include 'Keyboards.a'
	ENDIF
	IF &TYPE('__SOUND__') = 'UNDEFINED' THEN
	include 'Sound.a'
	ENDIF
	IF &TYPE('__OSA__') = 'UNDEFINED' THEN
	include 'OSA.a'
	ENDIF
	IF &TYPE('__OSACOMP__') = 'UNDEFINED' THEN
	include 'OSAComp.a'
	ENDIF
	IF &TYPE('__OSAGENERIC__') = 'UNDEFINED' THEN
	include 'OSAGeneric.a'
	ENDIF
	IF &TYPE('__APPLESCRIPT__') = 'UNDEFINED' THEN
	include 'AppleScript.a'
	ENDIF
	IF &TYPE('__ASDEBUGGING__') = 'UNDEFINED' THEN
	include 'ASDebugging.a'
	ENDIF
	IF &TYPE('__ASREGISTRY__') = 'UNDEFINED' THEN
	include 'ASRegistry.a'
	ENDIF
	IF &TYPE('__FINDERREGISTRY__') = 'UNDEFINED' THEN
	include 'FinderRegistry.a'
	ENDIF
	IF &TYPE('__PMAPPLICATION__') = 'UNDEFINED' THEN
	include 'PMApplication.a'
	ENDIF
	IF &TYPE('__NAVIGATION__') = 'UNDEFINED' THEN
	include 'Navigation.a'
	ENDIF
	IF &TYPE('__URLACCESS__') = 'UNDEFINED' THEN
	include 'URLAccess.a'
	ENDIF
	IF &TYPE('__COLORPICKER__') = 'UNDEFINED' THEN
	include 'ColorPicker.a'
	ENDIF
	IF &TYPE('__CMCALIBRATOR__') = 'UNDEFINED' THEN
	include 'CMCalibrator.a'
	ENDIF
	IF &TYPE('__HTMLRENDERING__') = 'UNDEFINED' THEN
	include 'HTMLRendering.a'
	ENDIF
	IF &TYPE('__SPEECHRECOGNITION__') = 'UNDEFINED' THEN
	include 'SpeechRecognition.a'
	ENDIF
	IF &TYPE('__NSL__') = 'UNDEFINED' THEN
	include 'NSL.a'
	ENDIF
	IF &TYPE('__KEYCHAINHI__') = 'UNDEFINED' THEN
	include 'KeychainHI.a'
	ENDIF
	IF &TYPE('__IBCARBONRUNTIME__') = 'UNDEFINED' THEN
	include 'IBCarbonRuntime.a'
	ENDIF
	IF &TYPE('__APPLEHELP__') = 'UNDEFINED' THEN
	include 'AppleHelp.a'
	ENDIF
	IF &TYPE('__ICAAPPLICATION__') = 'UNDEFINED' THEN
	include 'ICAApplication.a'
	ENDIF
	IF &TYPE('__ICADEVICE__') = 'UNDEFINED' THEN
	include 'ICADevice.a'
	ENDIF
	IF &TYPE('__ICACAMERA__') = 'UNDEFINED' THEN
	include 'ICACamera.a'
	ENDIF



	ENDIF ; __CARBON__ 

